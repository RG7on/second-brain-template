# Names of environment variables this repo's tooling expects — NEVER values.
# Real values live in macOS Keychain or your shell profile, not in git.
# (No variables required yet — MCP server credentials will be listed here
# when connections are added in a later phase.)
