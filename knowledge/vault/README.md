# vault/

Age-encrypted sensitive notes. Only `.age` ciphertext files belong here —
lint blocks plaintext notes in this folder. Setup, encrypt, decrypt, and
key-recovery instructions: `setup/runbooks/vault.md`.
